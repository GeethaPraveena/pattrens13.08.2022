#include<stdio.h>
void main()
{
int i,n,k,a[100];
scanf("%d",&n);
for(i=0;i<n;i++)
scanf("%d",&a[i]);
k=n-1;
array(a,0,k);
}
void array(int x[], int start, int end)
 {
 if(start>end)
  return;
 else
  printf("%d ",a[start]);
  array(a,start+1,end);
 }
