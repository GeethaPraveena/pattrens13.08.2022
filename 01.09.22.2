#include<stdio.h>
int fact(int);
void main()
{
    int x,ans;
    scanf("%d",&x);
    ans=fact(x);
    printf("%d",ans);
}
int fact(int x)
{
    int f=1;
    while(x!=0)
    {
        f=f*x;
        x--;
    }
    return(f);
}
