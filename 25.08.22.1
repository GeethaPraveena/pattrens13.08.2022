#include <stdio.h>
int main()
{
    int i,c=0;
    char a[100];
    printf("enter string\n");
    gets(a);
    for(i=0;a[i]!='\0';i++) 
    {
        if(a[i]==' ') 
        c=c+1;
    }
    printf("%d",c);
    

    return 0;
}
