#include <stdio.h>
int main()
{
   int n,a=0,b=1,i,c[100],count=0;
   scanf("%d",&n);
   c[0]=0;
   c[1]=1;
    for(i=2;i<=n;i++)
    {
        c[i]=c[0]+c[1];
        c[0]=c[1];
        c[1]=c[i+1];
    }
    for(i=0;i<=n;i++)
    {
        if(c[i]==n)
          count=1
    }
    if(count==1)
    printf("Yes");
    elsse
    printf("No");
    return 0;
}
