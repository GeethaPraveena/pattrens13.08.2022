#include<stdio.h>
int find_nth_term(int n, int a, int b, int c) {

    if(a==n)
    printf("%d",a);
    else if(b==n)
    printf("%d",a+b);
    else if(c==n)
    printf("%d",a+b+c);
    else if(n!=a&&n!=b&&n!=c)
    {
        
        
    }
}

int main() {
    int n, a, b, c;
  
    scanf("%d %d %d %d", &n, &a, &b, &c);
    int ans = find_nth_term(n, a, b, c);
 
    printf("%d", ans); 
    return 0;
}
