#include <stdio.h>
#include <string.h>
int main()
{
   char s1[30],s2[30];
   printf("Enter string 1:");
   gets(s1);
   printf("ENter string 2:");
   gets(s2);
   int n=strlen(s1);
   int m=strlen(s2);
   int i,j,count=0;
   for(i=0;i<n-1;i++)
   {
       for(j=0;j<n-i-1;j++)
       {
           char temp;
           if(s1[j]>s1[j+1]) 
           {
               temp=s1[j];
               s1[j]=s1[j+1];
               s1[j+1]=temp;
           }
       }
   }
   for(i=0;i<n-1;i++)
   {
       for(j=0;j<n-i-1;j++)
       {
           char temp1;
           if(s2[j]>s2[j+1]) 
           {
               temp1=s2[j];
               s2[j]=s2[j+1];
               s2[j+1]=temp1;
           }
       }
   }
   for(i=0;s1[i]!='\0';i++)
   {
       if(s1[i]==s2[i])
       count++;
   }
   if(count==n) 
   printf("anagram");
   else
   printf("not");
    return 0;
}
