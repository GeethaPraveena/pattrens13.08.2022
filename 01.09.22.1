#include <stdio.h>
int checkeveodd(int);
int main()
{
   int x,ans;
   scanf("%d",&x);
   ans=checkeveodd(x);
   if(ans==0)
   printf("even");
   else 
   printf("odd");
}
int checkeveodd(int x)
{
    if(x%2==0)
    return(0);
    else
    return(1);
}
