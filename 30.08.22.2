#include <stdio.h>
#include<string.h>
int main()
{
    int i;
    char a[100];
    gets(a);
    for(i=0;a[i]!=0;i++)
    {
        if(a[i]=='a')
        a[i]=a[i]-32;
    }
    puts(a);
    return 0;
}
